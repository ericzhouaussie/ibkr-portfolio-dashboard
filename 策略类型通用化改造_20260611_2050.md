# 策略类型通用化改造 - 完成报告

## 目标
将IBKR Portfolio Dashboard中的硬编码策略ID判断（wheel/leaps/dca/swing）改造为基于策略类型的通用判断，使用户在"新增策略"弹窗中选择"期权"/"正股"类型后，系统自动使用对应的持仓表单和数据逻辑。

## 完成内容

### 后端 (app.py)

1. **DEFAULT_STRATEGIES 添加 type 字段**
   - wheel/leaps → `"type": "option"`
   - dca/swing/cash → `"type": "stock"`

2. **新增 ```get_strategy_type()``` 辅助函数**
   - 按 strategy ID 查找 strategies 列表，返回 type
   - 向后兼容：wheel/leaps 默认返回 "option"，其他返回 "stock"
   - 用于所有需要判断策略类型的地方

3. **add_strategy API 接受 type 字段**
   - 新增策略 POST 路由接收并存储 type

4. **add_position() 使用策略类型**
   - 期权：统一字段处理（strike/expiry/contracts/buy_price/current_option_price/delta/stock_price）
   - 正股：DCA/Swing 字段处理（quantity/avg_price/current_price）
   - 合并逻辑：仅正股策略合并（期权不合并）
   - Wheel 特有字段（wheel_type/premium/cost_basis/status）向后兼容保留

5. **refresh_all_prices() 使用策略类型**
   - stock → 正股行情刷新
   - option → 期权行情刷新（Wheel特有计算+通用内在价值估算）

6. **close_position() 使用策略类型**
   - 拒绝非期权策略平仓
   - Wheel 特有字段处理（premium-based 空头盈亏）
   - 通用期权处理（buy_price-based 多头盈亏）
   - 平仓后现金自动管理

7. **update_option_price() 使用策略类型**
   - 拒绝非期权持仓
   - Wheel 特有：premium-based 盈亏计算
   - 通用期权：buy_price-based 盈亏计算

8. **开仓现金管理使用策略类型**
   - 期权：premium>0=卖期权收钱，buy_price>0=买期权付钱
   - 正股：买入股票/ETF 付钱

### 前端 (dashboard.js / dashboard.html) - 已有变更，已验证

- `getStrategyType()` 函数已存在
- 新增策略弹窗已包含"策略类型"选择器（option/stock）
- `renderAddPositionForm()` 已使用策略类型切换期权/正股表单
- `addPositionToStrategy()` 已使用统一的期权参数 POST
- `renderStrategies()` 期权渲染已基于策略类型
- 删除按钮仅显示在 cash/dca/wheel/leaps 之外
- `editPosition()` 已使用策略类型

## 验证
- Python 语法检查通过
- 服务启动正常（端口 5050）
- 向后兼容：现有 portfolio.json 中 wheel/leaps/dca/swing 策略不会丢失

## 未改动
- sell_position() 中 Swing 的 FIFO 卖出逻辑保持（因 Swing 特有 buy_trades 机制）
- 示例数据中的硬编码策略ID（sample data）
- 删除按钮排除逻辑保持 hardcoded（按需求仅移除 dca/wheel/leaps）
